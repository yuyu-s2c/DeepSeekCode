<Window x:Class="DeepSeekCode.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="DeepSeek Code"
        Height="800" Width="1100"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="White"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Style TargetType="FlowDocumentScrollViewer">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="260" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ═══ 左侧边栏 ═══ -->
            <Border x:Name="Sidebar"
                    Grid.Column="0"
                    Background="{StaticResource SurfaceTintBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12,10,8,12">

                        <!-- 📋 任务区域 -->
                        <TextBlock x:Name="SidebarTasksHeader"
                                   Text="📋 任务"
                                   Foreground="{StaticResource PrimaryDarkBrush}"
                                   FontSize="12" FontWeight="Bold"
                                   Margin="0,0,0,6"/>
                        <Border x:Name="TasksEmptyHint"
                                Background="{StaticResource SurfaceCardBrush}"
                                CornerRadius="4" Padding="10,8" Margin="0,0,0,6">
                            <TextBlock Text="暂无任务"
                                       Foreground="{StaticResource PrimaryLightBrush}"
                                       FontSize="11"/>
                        </Border>
                        <ItemsControl x:Name="SidebarTasksControl"
                                      Focusable="False"
                                      Visibility="Collapsed"/>

                        <!-- 分隔线 -->
                        <Border Height="1"
                                Background="{StaticResource BorderLightBrush}"
                                Margin="0,10,0,10"/>

                        <!-- 🤖 子代理区域 -->
                        <TextBlock x:Name="SidebarSubagentsHeader"
                                   Text="🤖 子代理"
                                   Foreground="{StaticResource PrimaryDarkBrush}"
                                   FontSize="12" FontWeight="Bold"
                                   Margin="0,0,0,6"/>
                        <Border x:Name="SubagentsEmptyHint"
                                Background="{StaticResource SurfaceCardBrush}"
                                CornerRadius="4" Padding="10,8" Margin="0,0,0,6">
                            <TextBlock Text="暂无子代理"
                                       Foreground="{StaticResource PrimaryLightBrush}"
                                       FontSize="11"/>
                        </Border>
                        <ItemsControl x:Name="SidebarSubagentControl"
                                      Focusable="False"
                                      Visibility="Collapsed"/>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- ═══ 分隔条 ═══ -->
            <GridSplitter x:Name="SidebarSplitter"
                          Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="Transparent"
                          ShowsPreview="True"
                          DragCompleted="SidebarSplitter_DragCompleted"/>

            <!-- ═══ 主内容区 ═══ -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ═══ 标题栏 ═══ -->
                <Border Grid.Row="0"
                        Background="{StaticResource SurfaceTintBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="10,7">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button x:Name="SidebarToggleButton"
                                    Content="☰"
                                    FontSize="14"
                                    Width="28" Height="26"
                                    Background="Transparent"
                                    Foreground="{StaticResource PrimaryLightBrush}"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="SidebarToggle_Click"
                                    ToolTip="切换侧边栏"
                                    VerticalAlignment="Center"
                                    Margin="0,0,4,0"/>
                            <TextBlock Text="📁" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Text="DeepSeek Code"
                                        Foreground="{StaticResource PrimaryBlueBrush}"
                                        FontSize="12" FontWeight="Bold"
                                        Margin="6,0,0,0" VerticalAlignment="Center"/>
                            <TextBlock Text=" — "
                                        Foreground="{StaticResource PrimaryLightBrush}"
                                        FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock x:Name="WorkspaceLabel"
                                        Text="..."
                                        Foreground="{StaticResource PrimaryLightBrush}"
                                        FontSize="11"
                                        FontFamily="Cascadia Code, Consolas, monospace"
                                        VerticalAlignment="Center"
                                        Cursor="Hand"
                                        MouseLeftButtonDown="WorkspaceBar_Click"
                                        ToolTip="点击切换工作区"/>
                        </StackPanel>

                        <Button Grid.Column="2"
                                x:Name="HistoryButton"
                                Content="💬 历史"
                                Click="HistoryButton_Click"
                                Height="26"
                                Background="Transparent"
                                Foreground="{StaticResource PrimaryLightBrush}"
                                FontSize="11"
                                BorderThickness="0"
                                Cursor="Hand"
                                VerticalAlignment="Center"
                                ToolTip="查看当前工作区的历史会话">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <!-- ═══ 主对话区 (WebView2) ═══ -->
                <wv2:WebView2 x:Name="ChatViewer" Grid.Row="1"/>

                <!-- ═══ 思考状态栏（仅流式输出中显示） ═══ -->
                <Border Grid.Row="2"
                        x:Name="ThinkingBar"
                        Background="{StaticResource SurfaceTintBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="14,8"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="ThinkingSpinner"
                                        Text="⣾"
                                        FontSize="15"
                                        Foreground="{StaticResource PrimaryBlueBrush}"
                                        VerticalAlignment="Center"
                                        FontFamily="Cascadia Code, Consolas, monospace"/>
                            <TextBlock x:Name="ThinkingIntentLabel"
                                        Text="AI 正在思考…"
                                        Foreground="{StaticResource PrimaryDarkBrush}"
                                        FontSize="13"
                                        VerticalAlignment="Center"
                                        Margin="10,0,0,0"
                                        TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                x:Name="StopButton"
                                Content="■ 停止生成"
                                Click="StopButton_Click"
                                Height="28"
                                Width="100"
                                Background="{StaticResource ErrorRedBrush}"
                                Foreground="White"
                                FontSize="12"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <!-- ═══ 输入区 ═══ -->
                <Border Grid.Row="3"
                        Background="{StaticResource SurfaceTintBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="12,10,12,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="⚙"
                                Click="SettingsButton_Click"
                                Width="36" Height="36"
                                Background="Transparent"
                                Foreground="{StaticResource PrimaryLightBrush}"
                                FontSize="18"
                                BorderThickness="0"
                                Cursor="Hand"
                                ToolTip="设置"
                                VerticalAlignment="Bottom"
                                Margin="0,0,6,0"/>

                        <Grid Grid.Column="1">
                            <TextBox x:Name="InputBox"
                                     MinHeight="36" MaxHeight="180"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     Background="White"
                                     Foreground="{StaticResource PrimaryDarkBrush}"
                                     BorderBrush="{StaticResource BorderDefaultBrush}"
                                     BorderThickness="1"
                                     FontSize="13"
                                     FontFamily="Microsoft YaHei"
                                     Padding="10,8"
                                     PreviewKeyDown="InputBox_PreviewKeyDown"
                                     TextChanged="InputBox_TextChanged"
                                     CaretBrush="{StaticResource PrimaryBlueBrush}"/>
                            <!-- / 命令补全弹出框 -->
                            <Popup x:Name="CommandPopup"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=InputBox}"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   Width="{Binding ActualWidth, ElementName=InputBox}">
                                <Border Background="White"
                                        BorderBrush="{StaticResource PrimaryBlueBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="4">
                                    <ListBox x:Name="CommandListBox"
                                             Background="Transparent"
                                             Foreground="{StaticResource PrimaryDarkBrush}"
                                             BorderThickness="0"
                                             MaxHeight="240"
                                             FontSize="13"
                                             FontFamily="Microsoft YaHei"
                                             KeyDown="CommandListBox_KeyDown"
                                             MouseDoubleClick="CommandListBox_MouseDoubleClick">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="10,6"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border x:Name="Border"
                                                                    Background="{TemplateBinding Background}"
                                                                    CornerRadius="4">
                                                                <ContentPresenter Margin="8,4"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="Border" Property="Background"
                                                                            Value="{StaticResource PrimaryBlueBrush}"/>
                                                                    <Setter TargetName="Border" Property="TextElement.Foreground"
                                                                            Value="White"/>
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Border" Property="Background"
                                                                            Value="{StaticResource SurfaceCardBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>
                                </Border>
                            </Popup>

                            <!-- @ 文件引用补全弹出框 -->
                            <Popup x:Name="FilePopup"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=InputBox}"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   Width="{Binding ActualWidth, ElementName=InputBox}">
                                <Border Background="White"
                                        BorderBrush="{StaticResource SuccessGreenBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="4">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                                   Text="📁 工作区文件"
                                                   Foreground="{StaticResource PrimaryLightBrush}"
                                                   FontSize="11"
                                                   Margin="8,4,8,2"/>
                                        <ListBox x:Name="FileListBox"
                                                 Grid.Row="1"
                                                 Background="Transparent"
                                                 Foreground="{StaticResource PrimaryDarkBrush}"
                                                 BorderThickness="0"
                                                 MaxHeight="240"
                                                 MinHeight="40"
                                                 FontSize="13"
                                                 FontFamily="Microsoft YaHei"
                                                 KeyDown="FileListBox_KeyDown"
                                                 MouseDoubleClick="FileListBox_MouseDoubleClick">
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="Padding" Value="10,6"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ListBoxItem">
                                                                <Border x:Name="Border"
                                                                        Background="{TemplateBinding Background}"
                                                                        CornerRadius="4">
                                                                    <ContentPresenter Margin="8,4"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsSelected" Value="True">
                                                                        <Setter TargetName="Border" Property="Background"
                                                                                Value="{StaticResource PrimaryBlueBrush}"/>
                                                                        <Setter TargetName="Border" Property="TextElement.Foreground"
                                                                                Value="White"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Border" Property="Background"
                                                                                Value="{StaticResource SurfaceCardBrush}"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                        </ListBox>
                                    </Grid>
                                </Border>
                            </Popup>
                        </Grid>

                        <Button x:Name="SendButton" Grid.Column="2"
                                Content="发送"
                                Click="SendButton_Click"
                                Width="80" Height="36"
                                Margin="8,0,0,0"
                                Background="{StaticResource PrimaryBlueBrush}"
                                Foreground="White"
                                FontSize="13"
                                BorderThickness="0"
                                Cursor="Hand"
                                VerticalAlignment="Bottom">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <!-- ═══ 状态栏 ═══ -->
                <Border Grid.Row="4"
                        Background="{StaticResource SurfaceTintBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="14,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="StatusModelLabel"
                                Text="deepseek-v4-pro"
                                Foreground="{StaticResource PrimaryBlueBrush}"
                                FontSize="13" FontWeight="SemiBold"
                                VerticalAlignment="Center"
                                Margin="0,0,20,0"/>
                    <TextBlock x:Name="PlanStatusLabel"
                                Text="🤖 自动模式"
                                Foreground="{StaticResource PrimaryLightBrush}"
                                FontSize="13"
                                VerticalAlignment="Center"
                                Margin="0,0,20,0"/>
                    <TextBlock x:Name="StatusContextLabel"
                                Text=""
                                Foreground="{StaticResource PrimaryLightBrush}"
                                FontSize="13"
                                VerticalAlignment="Center"
                                Margin="0,0,20,0"/>
                    <TextBlock x:Name="StatusTokenLabel"
                                    Text="Token: 0"
                                    Foreground="{StaticResource PrimaryLightBrush}"
                                    FontSize="13"
                                    VerticalAlignment="Center"
                                    Margin="0,0,20,0"/>
                        <TextBlock x:Name="StatusCacheLabel"
                                    Text=""
                                    Foreground="{StaticResource SuccessGreenBrush}"
                                    FontSize="13"
                                    VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusTimingLabel"
                                    Text=""
                                    Foreground="{StaticResource PrimaryLightBrush}"
                                    FontSize="13"
                                    VerticalAlignment="Center"
                                    Margin="20,0,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ═══ 历史会话弹窗 ═══ -->
            <Popup x:Name="HistoryPopup"
                Placement="Bottom"
                PlacementTarget="{Binding ElementName=HistoryButton}"
                StaysOpen="False"
                AllowsTransparency="True"
                Width="360">
                <Border Background="White"
                        BorderBrush="{StaticResource PrimaryBlueBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                    Text="历史会话"
                                    Foreground="{StaticResource PrimaryBlueBrush}"
                                    FontSize="12" FontWeight="Bold"
                                    Margin="8,6,8,2"/>

                        <ListBox x:Name="HistoryListBox"
                                Grid.Row="1"
                                Background="Transparent"
                                Foreground="{StaticResource PrimaryDarkBrush}"
                                BorderThickness="0"
                                MaxHeight="320"
                                MinHeight="60"
                                FontSize="13"
                                FontFamily="Microsoft YaHei"
                                KeyDown="HistoryListBox_KeyDown"
                                MouseDoubleClick="HistoryListBox_MouseDoubleClick">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10,6"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Border"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="4"
                                                        Padding="8,6">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background"
                                                                Value="{StaticResource PrimaryBlueBrush}"/>
                                                        <Setter TargetName="Border" Property="TextElement.Foreground"
                                                                Value="White"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background"
                                                                Value="{StaticResource SurfaceCardBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <TextBlock x:Name="HistoryEmptyLabel"
                                    Grid.Row="1"
                                    Text="暂无历史会话"
                                    Foreground="{StaticResource PrimaryLightBrush}"
                                    FontSize="12"
                                    Margin="10,16"
                                    Visibility="Collapsed"/>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="DeleteSessionButton"
                                    Content="删除"
                                    Click="DeleteSessionButton_Click"
                                    Height="30"
                                    Background="Transparent"
                                    Foreground="{StaticResource ErrorRedBrush}"
                                    FontSize="12"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Padding="10,0"/>
                            <Button Content="＋ 新建对话"
                                    Click="NewSessionButton_Click"
                                    Height="30"
                                    Background="Transparent"
                                    Foreground="{StaticResource PrimaryBlueBrush}"
                                    FontSize="12"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Padding="10,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Popup>
        </Grid>
    </Border>
</Window>
